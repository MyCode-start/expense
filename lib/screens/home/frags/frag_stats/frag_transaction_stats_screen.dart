
import 'package:flutter/material.dart';

class fragTransactionStats extends StatefulWidget {
  const fragTransactionStats({Key? key}) : super(key: key);

  @override
  State<fragTransactionStats> createState() => _fragTransactionStatsState();
}

class _fragTransactionStatsState extends State<fragTransactionStats> {
  

  @override
  void initState() {
    super.initState();
  }
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.orangeAccent,
      )

    );
  }
}
